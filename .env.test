# Database
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_USER=pact
DATABASE_PASSWORD=pact_dev_password
DATABASE_NAME=pact_test

# Application
NODE_ENV=test
PORT=3001

# LLM Configuration (use mocks in tests)
OPENAI_API_KEY=test-key
OPENAI_MODEL=gpt-4-turbo-preview

# Disable Redis-dependent LLM features for tests
# This prevents open handles from keeping Jest alive
LLM_CACHE_ENABLED=false
LLM_CIRCUIT_BREAKER_ENABLED=false
LLM_RATE_LIMIT_ENABLED=false

# Logging
LOG_LEVEL=error
